[alias]
    cp = cherry-pick
    cl = clone
    ci = commit
    co = checkout
    cob = checkout -b
    br = branch
    ds = diff --staged
    ll = log --graph --oneline --decorate
    st = status -s -b
    
